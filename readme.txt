Git is a version control system.
Git is free software.

git init
git add
git commit -m "XXX"
git status
git diff

版本回退
git log  // 提交历史 - 回退到某个历史版本
git reset --hard HEAD^  / HEAD^^ / HEAD~100 / 62a1c (commit id前几位)
git reflog // 历史命令 - 回到未来的某个版本